<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forum</title>
  <link rel="stylesheet" href="/styles/forum.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link rel="stylesheet" href="/styles/forum.css">
  
</head>
<body>
  <div class="forum-container">
    <h1>Active Forums</h1>

    <% if (posts.length > 0) { %>
      <% posts.forEach(post => { %>
        <div class="forum-item">
          <div class="forum-content">
            <a href="/post/<%= post.slug %>/comments" class="comments-link"></a>
            <h3><%= post.title %></h3>
            <span>Comments: <%= post.commentCount || 0 %></span><br>
            <a href="/post/<%= post.slug %>/comments" class="comments-link">View or Add Comments</a>s
          </div>
          <div class="forum-activity">
            
            <div class="like-dislike">
              <button class="like-button" data-post-id="<%= post._id %>">
                <i class="fas fa-thumbs-up"></i> Like (<%= post.likeCount || 0 %>)
              </button>
              <button class="dislike-button" data-post-id="<%= post._id %>">
                <i class="fas fa-thumbs-down"></i> Dislike (<%= post.dislikeCount || 0 %>)
              </button>
            </div>
            
          </div>
        </div>
      <% }); %>
    <% } else { %>
      <p>No forums available at the moment.</p>
    <% } %>
  </div>

</body>
</html>
